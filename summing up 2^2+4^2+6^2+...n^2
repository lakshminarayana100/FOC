#include <stdio.h>

int main() {
    int n;
    int sum = 0;

    printf("Enter a number: ");
    scanf("%d", &n);

    for (int i = 2; i <= n; i += 2) {
        sum += i * i;
    }

    printf("Sum of squares of even numbers from 2 to %d: %d\n", n, sum);

    return 0;
}
